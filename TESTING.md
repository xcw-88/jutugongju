# 测试与验收

## 自动验证

命令：`python -m pytest -q`。

V1.3.1 将软件显示名称、激活界面、PDF 作者和打包文件统一改为「聊天软件截屏助手」；内部授权产品标识及本机配置目录保持不变，确保既有授权和设置继续有效。

已通过 34 个测试用例，覆盖：首次截图前等待、每次滚动后等待、最后一张后不滚动、停止前后文件保留、滚轮故障中止、防止覆盖原图、同名目录及 Windows 非法文件名、完全相同截图保留、A4 页面尺寸、中文封面、双向页序、PDF 内嵌图片逐像素一致、再次导出不覆盖、原图变更/缺失校验、长说明续页、空目录、无效框选、100%/125%/150%/200% 缩放和负屏幕坐标换算，以及 Windows 滚轮 120 单位/格的换算与鼠标移入选区。

V1.1 新增验证：向下滚轮发送负值（3 格为 -360）、向下采集的等待与末张停止滚动、方向记录及底部提示、旧配置默认向上、界面切换方向同步默认 PDF 页序、方向保存/恢复、手动覆盖 PDF 页序、重开证据目录时恢复该目录方向和上次页序。界面集成测试以 offscreen 方式运行，不操作真实桌面。

V1.1 打包后另在 Windows 11 实际启动 EXE，确认「滚动方向」提供向上/向下，选中向下后 PDF 顺序自动切换为 001、002、003。真实钉钉的向下滚动仍需按下方验收表实测。

V1.2 新增验证：自定义、长标题、空标题的封面与截图页及文件名；无原图文件名和打印页码；标题与自动结束设置保存/恢复；重开目录恢复上次导出的标题。向上、向下连续两次画面未变化时提前结束、保留全部三张截图并记录结束原因；画面变化时重置计数；关闭自动结束后采满数量上限。界面集成测试确认提前结束后恢复主界面、恢复按钮并安排自动生成 PDF。边界条件使用模拟截图验证，尚未完成真实钉钉边界实测。

V1.2 打包后在 Windows 11 实际启动根目录 EXE，确认启动遮罩、Esc 返回主界面、V1.2 版本标识、可编辑材料标题及默认勾选的自动结束选项均正常显示；沿用既有截图区域、向下方向与输出目录。

## PDF 视觉检查

使用明确标注为模拟内容的 960 × 1200 PNG 生成测试 PDF，使用 Poppler 渲染封面与截图页，检查中文、边距、比例及无遮挡。V1.2 已检查普通自定义标题和长标题的封面及截图页，确认标题完整换行、截图完整，页面未添加原图文件名及页码。新版测试文件位于 `tmp/qa/v12`，不是实际证据材料。

## V1.2.1 微信滚动修复验证

- 自动测试确认双向逐格发送 ±120 的 Windows 滚轮输入；多格滚动中用户停止或窗口失去焦点时，不再发送后续滚轮，已写入截图保留。
- 在本机微信 4.1.11.55 的当前聊天中运行 V1.2.1 打包 EXE，向下 6 格、加载等待 3 秒、采集 3 张。三张 PNG 均为 550 × 853 像素，采集正常完成并恢复主界面。本次关闭自动 PDF，仅验证滚动和截图。
- 对比旧版该微信任务的前两次滚动，均仅位移 50 像素；新版三张截图的相邻位移为 401、487 像素。通过聊天区重叠部分逐像素比较确认（排除固定标题和滚动条），重叠部分一致，截图目视检查通过。
- 验证文件单独存放在 `tmp/qa/wechat-compat`，含实际聊天内容，仅用于本机排查，不是模拟数据，不应随程序分发。原证据目录未改动。
- 本次没有覆盖微信全部历史、顶部/底部边界或不同微信版本；实际位移会受软件滚动行为影响，升级后先试采确认上下文重叠。

## V1.0 打包 EXE 的桌面测试

2026-09-03 在 Windows 11 上运行 V1.0 打包的 `EvidenceCapture.exe`，完成启动遮罩、Esc 取消并保留配置、填写名称、倒计时、MSS 截图、真实 Windows 鼠标滚轮及自动 PDF 导出。

- 使用独立进程的模拟聊天窗口，已在窗口内显著标注「软件测试专用 / 非真实证据」。
- 实际框选位置为 `(272, 96)`，区域 `946 × 813` 像素；最终保存的三张 PNG 尺寸均一致。
- 开始时间 20:22:52，截图时间 20:23:00、20:23:03、20:23:06（UTC+08:00），对应 5 秒倒计时、3 秒加载等待。
- 三张 SHA-256 均不相同；检查第 001、003 张可见聊天由第 057–060 条向第 056 条方向推进，保留重叠内容，无重复警告。
- 20:23:07 自动完成 PDF：共 4 页，封面一页，截图按 003、002、001 排序。再次用 Poppler 渲染截图页检查；PDF 内每张图像仍为 `946 × 813` 像素。
- 开发期间实际排查并修复了两类故障：打包误收集 Poppler 的同名 ICU DLL；PyAutoGUI 在 Windows 上未移动光标且需要将格数换算为 120 单位的滚轮输入。

## 实际钉钉验收表

开发时钉钉窗口可枚举，但桌面自动化接口持续返回窗口归属绑定错误，刷新后仍失败。因此以下聊天尚未完成实测，不将模拟测试视为真实钉钉验收。

| 对象 | 图片懒加载 | 长聊天滚动与上下文重叠 | A4 实际打印 | 状态 |
|---|---|---|---|---|
| 陈庆 | 待测 | 待测 | 待测 | 未完成真实聊天验收 |
| 叶亚运 | 待测 | 待测 | 待测 | 未完成真实聊天验收 |
| 吴森 | 待测 | 待测 | 待测 | 未完成真实聊天验收 |
| 监理 | 待测 | 待测 | 待测 | 未完成真实聊天验收 |

每个对象先试采 3 张，确认相邻截图重叠、图片已加载、文字没有截断；再采集 50 张测试长聊天（运行时按 Esc 可停止）。最后用 A4 竖版试印封面及包含最小文字/图片的一页，检查文字可辨和页序。没有实际连接打印机，因此未声称完成纸质打印验证。

## 兼容性范围

本次开发机是 Windows 11 x64、1920 × 1080 单显示器、100% 缩放。其他缩放及负坐标通过计算测试覆盖，真实多屏、混合缩放和 Windows 10 仍需实机验证。屏幕图片的清晰度受源画面影响。
